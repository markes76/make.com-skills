# Make Automation Guru

Read `SKILL.md` before designing or changing a Make automation. Use the Make MCP connector as the live schema and control plane. Never guess module names, fields, connections, or webhook schemas. Keep new scenarios inactive unless activation is explicitly requested.

When MCP is unavailable or insufficient, return a documented design and identify the official-source research needed; do not fabricate API behavior.
